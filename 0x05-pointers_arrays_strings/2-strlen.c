#include "holberton.h"
#include <string.h>

/**
 * _strlen - return the length of a string
 *
 * @s: Is a character
 * Return: 0 (Succes)
 */

int _strlen(char *s)
{
	int len;

	for (len = 0; s[len] != '\0'; len++)
	{
	}
	return (len);
}
