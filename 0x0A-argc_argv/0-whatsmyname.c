#include "holberton.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * main - program that prints the its name
 * @argc: count argument
 * @argv: vector argument
 * Return: 0 (Success)
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	if (argc)
	{}
	return (0);
}
