#include "holberton.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * main -prints the number of arguments passed into it
 * @argc: count argument
 * @argv: vector argument
 * Return: 0 (Success)
 */
int main(int argc, char *argv[])
{
	printf("%d\n", argc - 1);
	if (argv)
	{}
	return (0);
}
